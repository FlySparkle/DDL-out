# 旧目录 Codex 任务索引

## 使用说明

这些任务来自旧工作区
`C:\Users\27950\Desktop\Work\Python面向对象\DDL out！`。索引用于在需要追溯
设计原因、故障处理过程或用户原始意图时定位原对话，不是当前需求清单。

如果本机 Codex 仍保留任务记录，可使用任务 ID 查阅。若记录不可访问，应以
[项目交接摘要](../PROJECT_HANDOFF.md)、当前代码、测试和 ADR 为准。索引不复制
原始聊天全文，也不记录数据库、签名材料、代理信息或其他本地敏感内容。

## 任务列表

### 1. 初始项目规范化与旧版功能整理

- **任务 ID**：`019f5100-5a06-7db2-a927-d4ccf5c454fb`
- **原始标题**：`先研读整个项目的git仓库等代码变更，理解项目过去。接下来我要将这个项目制作成规范的发布到github的开源项目。现在…`
- **核心成果**：保存早期 Flask/WebView 可运行基线；清理当时已放弃的移动端/PWA
  痕迹；完成相对时间自动进位和截止时间滚轮加直接输入的双向同步。
- **有效性**：产品交互动机可参考；Python/WebView 架构和固定窗口方案已被 Flutter
  重写取代。
- **建议回查**：需要理解旧版截止时间输入、早期产品行为或迁移前仓库历史时。

### 2. Flutter 重写、环境建设与首版交付

- **任务 ID**：`019f522b-74e7-7321-a3ea-ab7c476c60f1`
- **原始标题**：`打算用dart+flutter重写项目。给个路线图`
- **核心成果**：形成 Flutter 全平台重写规格；建立 Riverpod、Drift、Material 3
  架构；配置 Windows 和 Android 工具链；完成早期 Windows/Android 构建与测试；
  记录中文路径、代码生成、插件链接、AGP/Kotlin 兼容性和签名问题。
- **有效性**：架构原则和故障经验仍有参考价值；其中具体版本、产物哈希、旧路径和
  “首版不做同步”等范围只代表当时状态。
- **建议回查**：需要理解 Flutter 迁移动机、工具链由来、旧路径构建失败或插件
  兼容性选择时。

### 3. UI、导航与交互逻辑调整

- **任务 ID**：`019f5794-6475-7050-a811-3a6b1b447283`
- **原始标题**：`1.如图二，参考微软设置的侧边抽屉，右上角的设置移动到该处作为其中的一项。2.设置中新增字体字号个性化设置。其中除了w…`
- **核心成果**：设置入口迁入导航抽屉；增加字体和字号设置；分类改为原生彩色
  卡片；实现分类拖拽排序与单分类清空；提高输入框对比度；实现并修复桌面响应式
  侧栏；识别并忽略旧嵌套构建残留。
- **有效性**：多数成果仍在当前产品中；最初仅面向 PC 的侧栏开关已被 ADR 004
  的跨平台响应式导航方案取代。
- **建议回查**：需要追溯现有视觉和导航选择、侧栏内容区消失回归测试或英文路径
  迁移原因时。

### 4. Git 与发版命令讲解

- **任务 ID**：`019f5817-791e-7972-936d-e9b6306d60e3`
- **原始标题**：`发版`
- **核心成果**：解释工作区、暂存区、本地仓库和远程仓库的关系；逐条说明
  `fetch`、`status`、`push`、Flutter 检查、版本提交、PR、标签和 Release 流程。
- **有效性**：基础 Git 概念仍有效；具体分支名、版本号和当时的未提交文件已经
  过时。
- **建议回查**：需要了解用户期望的发版教学粒度，或追溯标签触发自动发布的原始
  说明时。

### 5. v0.2.0 发版实操与英文目录迁移

- **任务 ID**：`019f5836-30cc-79c2-a881-ebe71791c142`
- **原始标题**：`发版 (2)`
- **核心成果**：把仓库迁移到 `C:\Users\27950\Desktop\Work\DDL-out`；完成
  `v0.2.0` PR、版本冲突处理、标签和自动 Release 触发；区分本地构建产物与正式
  Actions 签名产物；记录依赖下载和代理端口故障。
- **有效性**：英文目录和标签驱动发布原则仍有效；`v0.2.0`、旧功能分支、代理端口
  和当时的构建状态仅是历史信息。
- **建议回查**：需要理解为什么新目录是唯一工作区、如何处理版本行冲突或如何
  补打已合并版本的标签时。

### 6. 局域网扫码同步、合并与冲突审批

- **任务 ID**：`019f887a-01f5-7053-acc3-c27680a2fc75`
- **原始标题**：`我需要更新远程多端同步的功能，给我提点建议，到底是用账户注册呢还是绑定API或者token呢还是什么别的`
- **核心成果**：比较账号、API/token、GitHub 与免费同步方案；选择不依赖云服务的
  局域网二维码直连；设计版本向量、操作日志、字段级合并、墓碑删除和显式冲突；
  确定生成二维码的电脑默认审批、手机可接管；同步实现从旧目录迁移到英文目录。
- **有效性**：当前进行中的产品方向，正式细节以
  [ADR 002](../decisions/002-lan-peer-sync-and-conflicts.md) 和当前未提交代码为准。
- **建议回查**：需要追溯同步方案比较、冲突模型、审批设备选择、UI 要求或代码
  迁移过程时。

## 维护规则

- 这里只增加真正影响长期理解的历史任务，不把普通问答或重复日志逐条归档。
- 旧任务中的结论被代码或 ADR 取代时，更新“有效性”，不要删除历史记录。
- 新的长期决定应写入 ADR；本索引只负责定位来源，不替代架构文档。
